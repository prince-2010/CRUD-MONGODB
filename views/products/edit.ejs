<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EDIT PRODUCT</title>
</head>
<body>
    <h1>Edit</h1>
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <label for="name">Product Name: </label>
        <br>
        <textarea name="name" id="name" cols="30" rows="2">
<%=product.name%>
        </textarea>
        <br>
        <label for="p">Enter Price: </label>
        <br>
       <input type="number" name ="price" value="<%=product.price%>">
        <br>
        <label for="cat">Choose Categry: </label>
        <br>
        <select name="category" id="cat">
            <%for(let category of categories){ %>
                <option   <%=product.category === category?'selected': '' %> value="<%= category %>"><%= category %></option>
            <% } %>
        </select>
        <button>Save</button>
    </form>
    <br>
    <a href="/products/<%=product._id%>">Back</a>
</body>
</html>